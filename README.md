# NoteFlow

SPA для заметок с todo-списками (Nuxt 4, Pinia, localStorage). Две страницы: список заметок и создание/редактирование с
undo/redo.

**Запуск:** `npm install && npm run dev`  
**Сборка в Docker:** `docker-compose up`
